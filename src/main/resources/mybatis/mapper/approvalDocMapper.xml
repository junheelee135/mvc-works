<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mvc.app.mapper.ApprovalDocMapper">

    <!-- 기안서 등록 (selectKey로 docId를 미리 받아옴) -->
    <insert id="insertDoc" parameterType="com.mvc.app.domain.dto.ApprovalDocDto">
        <selectKey keyProperty="docId" resultType="long" order="BEFORE">
            SELECT approvalDoc_seq.NEXTVAL FROM dual
        </selectKey>
        INSERT INTO approvalDoc (
            docId, docTypeId, title, content, detailData,
            docStatus, writerEmpId, writerEmpName,
            writerDeptCode, writerDeptName,
            writerGradeCode, writerGradeName, regDate, submitDate
        ) VALUES (
            #{docId}, #{docTypeId}, #{title},
            #{content, jdbcType=VARCHAR},
            #{detailData, jdbcType=VARCHAR},
            #{docStatus}, #{writerEmpId}, #{writerEmpName},
            #{writerDeptCode, jdbcType=VARCHAR},
            #{writerDeptName, jdbcType=VARCHAR},
            #{writerGradeCode, jdbcType=VARCHAR},
            #{writerGradeName, jdbcType=VARCHAR},
            SYSDATE,
            CASE WHEN #{docStatus} = 'PENDING' THEN SYSDATE ELSE NULL END
        )        
    </insert>

    <!-- 결재선 등록 -->
    <insert id="insertLine" parameterType="com.mvc.app.domain.dto.ApprovalLineDto">
        INSERT INTO approvalLine (
            lineId, docId, stepOrder,
            apprEmpId, apprEmpName,
            apprDeptCode, apprDeptName,
            apprGradeCode, apprGradeName,
            apprTypeCode, lineStatusCode
        ) VALUES (
            approvalLine_seq.NEXTVAL, #{docId}, #{stepOrder},
            #{apprEmpId}, #{apprEmpName},
            #{apprDeptCode}, #{apprDeptName},
            #{apprGradeCode}, #{apprGradeName},
            'APPROVER', 'WAIT'
        )
    </insert>


    <!-- 참조자 등록 -->
    <insert id="insertRef" parameterType="com.mvc.app.domain.dto.ApprovalRefDto">
        INSERT INTO approvalReference (
            refId, docId, refEmpId, refEmpName,
            refDeptCode, refDeptName,
            refGradeCode, refGradeName
        ) VALUES (
            approvalReference_seq.NEXTVAL, #{docId},
            #{refEmpId}, #{refEmpName},
            #{refDeptCode}, #{refDeptName},
            #{refGradeCode}, #{refGradeName}
        )
    </insert>

	<!-- 첨부파일 -->
    <insert id="insertFile" parameterType="com.mvc.app.domain.dto.ApprovalFileDto">
        INSERT INTO approvalFile (fileId, docId, oriFilename, saveFilename, fileSize, regDate)
        VALUES (approvalFile_seq.NEXTVAL, #{docId}, #{oriFilename}, #{saveFilename}, #{fileSize}, SYSDATE)
    </insert>

    <!-- 임시저장 목록 -->
    <select id="listDraft" parameterType="map" resultType="com.mvc.app.domain.dto.ApprovalDocDto">
        SELECT d.docId, d.title, d.docStatus,
               d.writerEmpName, d.writerGradeName,
               d.regDate, t.typeName
        FROM approvalDoc d
        LEFT JOIN approvalDocType t ON d.docTypeId = t.docTypeId
        WHERE d.writerEmpId = #{empId}
          AND d.docStatus = 'DRAFT'
        <if test="keyword != null and keyword != ''">
            AND d.title LIKE '%' || #{keyword} || '%'
        </if>
        <if test="startDate != null and startDate != ''">
            AND d.regDate &gt;= TO_DATE(#{startDate}, 'YYYY-MM-DD')
        </if>
        <if test="endDate != null and endDate != ''">
            AND d.regDate &lt; TO_DATE(#{endDate}, 'YYYY-MM-DD') + 1
        </if>
        ORDER BY d.docId DESC
        OFFSET #{offset} ROWS FETCH NEXT #{pageSize} ROWS ONLY
    </select>
    
    <!-- 임시저장 건수 -->
    <select id="countDraft" parameterType="map" resultType="int">
        SELECT COUNT(*)
        FROM approvalDoc d
        WHERE d.writerEmpId = #{empId}
          AND d.docStatus = 'DRAFT'
        <if test="keyword != null and keyword != ''">
            AND d.title LIKE '%' || #{keyword} || '%'
        </if>
        <if test="startDate != null and startDate != ''">
            AND d.regDate &gt;= TO_DATE(#{startDate}, 'YYYY-MM-DD')
        </if>
        <if test="endDate != null and endDate != ''">
            AND d.regDate &lt; TO_DATE(#{endDate}, 'YYYY-MM-DD') + 1
        </if>
    </select>
    
  <!-- 보낸 결재함 목록 -->
  <select id="listSent" parameterType="map" resultType="com.mvc.app.domain.dto.ApprovalDocDto">
      SELECT d.docId, d.title, d.docStatus,
             d.writerEmpName, d.writerGradeName,
             d.regDate, t.typeName,
             (SELECT COUNT(*) FROM approvalLine WHERE docId = d.docId AND lineStatusCode = 'APPROVED') AS approvedCount,
             (SELECT COUNT(*) FROM approvalLine WHERE docId = d.docId) AS totalLineCount
      FROM approvalDoc d
      LEFT JOIN approvalDocType t ON d.docTypeId = t.docTypeId
      WHERE d.writerEmpId = #{empId}
        AND d.docStatus != 'DRAFT'
      <if test="keyword != null and keyword != ''">
          AND d.title LIKE '%' || #{keyword} || '%'
      </if>
      <if test="startDate != null and startDate != ''">
          AND d.regDate &gt;= TO_DATE(#{startDate}, 'YYYY-MM-DD')
      </if>
      <if test="endDate != null and endDate != ''">
          AND d.regDate &lt; TO_DATE(#{endDate}, 'YYYY-MM-DD') + 1
      </if>
      ORDER BY d.docId DESC
      OFFSET #{offset} ROWS FETCH NEXT #{pageSize} ROWS ONLY
  </select>

  <select id="countSent" parameterType="map" resultType="int">
      SELECT COUNT(*)
      FROM approvalDoc d
      WHERE d.writerEmpId = #{empId}
        AND d.docStatus != 'DRAFT'
      <if test="keyword != null and keyword != ''">
          AND d.title LIKE '%' || #{keyword} || '%'
      </if>
      <if test="startDate != null and startDate != ''">
          AND d.regDate &gt;= TO_DATE(#{startDate}, 'YYYY-MM-DD')
      </if>
      <if test="endDate != null and endDate != ''">
          AND d.regDate &lt; TO_DATE(#{endDate}, 'YYYY-MM-DD') + 1
      </if>
  </select>

  <!-- 받은 결재함 목록 (내 차례 WAIT + 내가 승인한 APPROVED + 대결) -->
  <select id="listInbox" parameterType="map" resultType="com.mvc.app.domain.dto.ApprovalDocDto">
      SELECT d.docId, d.title, d.docStatus,
             d.writerEmpName, d.writerGradeName,
             d.regDate, t.typeName,
             l.lineStatusCode AS myLineStatus,
             (SELECT COUNT(*) FROM approvalLine WHERE docId = d.docId AND lineStatusCode = 'APPROVED') AS approvedCount,
             (SELECT COUNT(*) FROM approvalLine WHERE docId = d.docId) AS totalLineCount
      FROM approvalDoc d
      LEFT JOIN approvalDocType t ON d.docTypeId = t.docTypeId
      INNER JOIN approvalLine l ON d.docId = l.docId
      WHERE (l.apprEmpId = #{empId} OR l.deputyEmpId = #{empId})
        AND (
            (d.docStatus = 'PENDING' AND l.lineStatusCode = 'WAIT'
             AND l.stepOrder = (
                 SELECT MIN(stepOrder) FROM approvalLine
                 WHERE docId = d.docId AND lineStatusCode = 'WAIT'))
            OR
            (l.lineStatusCode = 'APPROVED')
        )
      <if test="keyword != null and keyword != ''">
          AND d.title LIKE '%' || #{keyword} || '%'
      </if>
      <if test="startDate != null and startDate != ''">
          AND d.regDate &gt;= TO_DATE(#{startDate}, 'YYYY-MM-DD')
      </if>
      <if test="endDate != null and endDate != ''">
          AND d.regDate &lt; TO_DATE(#{endDate}, 'YYYY-MM-DD') + 1
      </if>
      ORDER BY d.docId DESC
      OFFSET #{offset} ROWS FETCH NEXT #{pageSize} ROWS ONLY
  </select>

  <select id="countInbox" parameterType="map" resultType="int">
      SELECT COUNT(*)
      FROM approvalDoc d
      INNER JOIN approvalLine l ON d.docId = l.docId
      WHERE (l.apprEmpId = #{empId} OR l.deputyEmpId = #{empId})
        AND (
            (d.docStatus = 'PENDING' AND l.lineStatusCode = 'WAIT'
             AND l.stepOrder = (
                 SELECT MIN(stepOrder) FROM approvalLine
                 WHERE docId = d.docId AND lineStatusCode = 'WAIT'))
            OR
            (l.lineStatusCode = 'APPROVED')
        )
      <if test="keyword != null and keyword != ''">
          AND d.title LIKE '%' || #{keyword} || '%'
      </if>
      <if test="startDate != null and startDate != ''">
          AND d.regDate &gt;= TO_DATE(#{startDate}, 'YYYY-MM-DD')
      </if>
      <if test="endDate != null and endDate != ''">
          AND d.regDate &lt; TO_DATE(#{endDate}, 'YYYY-MM-DD') + 1
      </if>
  </select>

  <!-- 미결재 문서 (내 차례 WAIT + 대결 대상 WAIT) -->
  <select id="listPendingInbox" parameterType="map" resultType="com.mvc.app.domain.dto.ApprovalDocDto">
      SELECT d.docId, d.title, d.docStatus,
             d.writerEmpName, d.writerGradeName,
             d.regDate, t.typeName
      FROM approvalDoc d
      LEFT JOIN approvalDocType t ON d.docTypeId = t.docTypeId
      INNER JOIN approvalLine l ON d.docId = l.docId
      WHERE (
              l.apprEmpId = #{empId}
              OR l.apprEmpId IN (
                  SELECT delegatorEmpId FROM approvalDeputyReg
                  WHERE deputyEmpId = #{empId}
                    AND isActive = 'Y'
                    AND TRUNC(SYSDATE) BETWEEN startDate AND endDate
              )
            )
        AND d.docStatus = 'PENDING'
        AND l.lineStatusCode = 'WAIT'
        AND l.stepOrder = (
            SELECT MIN(stepOrder) FROM approvalLine
            WHERE docId = d.docId AND lineStatusCode = 'WAIT'
        )
      <if test="keyword != null and keyword != ''">
          AND d.title LIKE '%' || #{keyword} || '%'
      </if>
      <if test="startDate != null and startDate != ''">
          AND d.regDate &gt;= TO_DATE(#{startDate}, 'YYYY-MM-DD')
      </if>
      <if test="endDate != null and endDate != ''">
          AND d.regDate &lt; TO_DATE(#{endDate}, 'YYYY-MM-DD') + 1
      </if>
      ORDER BY d.docId DESC
      OFFSET #{offset} ROWS FETCH NEXT #{pageSize} ROWS ONLY
  </select>

  <select id="countPendingInbox" parameterType="map" resultType="int">
      SELECT COUNT(*)
      FROM approvalDoc d
      INNER JOIN approvalLine l ON d.docId = l.docId
      WHERE (
              l.apprEmpId = #{empId}
              OR l.apprEmpId IN (
                  SELECT delegatorEmpId FROM approvalDeputyReg
                  WHERE deputyEmpId = #{empId}
                    AND isActive = 'Y'
                    AND TRUNC(SYSDATE) BETWEEN startDate AND endDate
              )
            )
        AND d.docStatus = 'PENDING'
        AND l.lineStatusCode = 'WAIT'
        AND l.stepOrder = (
            SELECT MIN(stepOrder) FROM approvalLine
            WHERE docId = d.docId AND lineStatusCode = 'WAIT'
        )
      <if test="keyword != null and keyword != ''">
          AND d.title LIKE '%' || #{keyword} || '%'
      </if>
      <if test="startDate != null and startDate != ''">
          AND d.regDate &gt;= TO_DATE(#{startDate}, 'YYYY-MM-DD')
      </if>
      <if test="endDate != null and endDate != ''">
          AND d.regDate &lt; TO_DATE(#{endDate}, 'YYYY-MM-DD') + 1
      </if>
  </select>

  <!-- 미확인 문서 (참조 중 readYn='N') -->
  <select id="listUnreadRef" parameterType="map" resultType="com.mvc.app.domain.dto.ApprovalDocDto">
      SELECT d.docId, d.title, d.docStatus,
             d.writerEmpName, d.writerGradeName,
             d.regDate, t.typeName, r.readYn
      FROM approvalDoc d
      LEFT JOIN approvalDocType t ON d.docTypeId = t.docTypeId
      INNER JOIN approvalReference r ON d.docId = r.docId
      WHERE r.refEmpId = #{empId}
        AND d.docStatus NOT IN ('DRAFT', 'CANCELED')
        AND r.readYn = 'N'
      <if test="keyword != null and keyword != ''">
          AND d.title LIKE '%' || #{keyword} || '%'
      </if>
      <if test="startDate != null and startDate != ''">
          AND d.regDate &gt;= TO_DATE(#{startDate}, 'YYYY-MM-DD')
      </if>
      <if test="endDate != null and endDate != ''">
          AND d.regDate &lt; TO_DATE(#{endDate}, 'YYYY-MM-DD') + 1
      </if>
      ORDER BY d.docId DESC
      OFFSET #{offset} ROWS FETCH NEXT #{pageSize} ROWS ONLY
  </select>

  <select id="countUnreadRef" parameterType="map" resultType="int">
      SELECT COUNT(*)
      FROM approvalDoc d
      INNER JOIN approvalReference r ON d.docId = r.docId
      WHERE r.refEmpId = #{empId}
        AND d.docStatus NOT IN ('DRAFT', 'CANCELED')
        AND r.readYn = 'N'
      <if test="keyword != null and keyword != ''">
          AND d.title LIKE '%' || #{keyword} || '%'
      </if>
      <if test="startDate != null and startDate != ''">
          AND d.regDate &gt;= TO_DATE(#{startDate}, 'YYYY-MM-DD')
      </if>
      <if test="endDate != null and endDate != ''">
          AND d.regDate &lt; TO_DATE(#{endDate}, 'YYYY-MM-DD') + 1
      </if>
  </select>

  <!-- 참조자 읽음 처리 -->
  <update id="markRefAsRead" parameterType="map">
      UPDATE approvalReference
      SET readYn = 'Y', readDate = SYSDATE
      WHERE docId = #{docId} AND refEmpId = #{empId} AND readYn = 'N'
  </update>

  <select id="listRef" parameterType="map" resultType="com.mvc.app.domain.dto.ApprovalDocDto">
        SELECT d.docId, d.title, d.docStatus,
               d.writerEmpName, d.writerGradeName,
               d.regDate, t.typeName, r.readYn
        FROM approvalDoc d
        LEFT JOIN approvalDocType t ON d.docTypeId = t.docTypeId
        INNER JOIN approvalReference r ON d.docId = r.docId
        WHERE r.refEmpId = #{empId}
          AND d.docStatus NOT IN ('DRAFT', 'CANCELED')
        <if test="keyword != null and keyword != ''">
            AND d.title LIKE '%' || #{keyword} || '%'
        </if>
        <if test="startDate != null and startDate != ''">
            AND d.regDate &gt;= TO_DATE(#{startDate}, 'YYYY-MM-DD')
        </if>
        <if test="endDate != null and endDate != ''">
            AND d.regDate &lt; TO_DATE(#{endDate}, 'YYYY-MM-DD') + 1
        </if>
        ORDER BY d.docId DESC
        OFFSET #{offset} ROWS FETCH NEXT #{pageSize} ROWS ONLY
    </select>

    <select id="countRef" parameterType="map" resultType="int">
        SELECT COUNT(*)
        FROM approvalDoc d
        INNER JOIN approvalReference r ON d.docId = r.docId
        WHERE r.refEmpId = #{empId}
          AND d.docStatus NOT IN ('DRAFT', 'CANCELED')
        <if test="keyword != null and keyword != ''">
            AND d.title LIKE '%' || #{keyword} || '%'
        </if>
        <if test="startDate != null and startDate != ''">
            AND d.regDate &gt;= TO_DATE(#{startDate}, 'YYYY-MM-DD')
        </if>
        <if test="endDate != null and endDate != ''">
            AND d.regDate &lt; TO_DATE(#{endDate}, 'YYYY-MM-DD') + 1
        </if>
    </select>

    <select id="listAll" parameterType="map" resultType="com.mvc.app.domain.dto.ApprovalDocDto">
        SELECT DISTINCT d.docId, d.title, d.docStatus,
               d.writerEmpName, d.writerGradeName,
               d.regDate, t.typeName,
               (SELECT lineStatusCode FROM approvalLine
                WHERE docId = d.docId AND apprEmpId = #{empId}
                AND ROWNUM = 1) AS myLineStatus,
               (SELECT COUNT(*) FROM approvalLine
                WHERE docId = d.docId AND lineStatusCode = 'APPROVED') AS approvedCount,
               (SELECT COUNT(*) FROM approvalLine
                WHERE docId = d.docId) AS totalLineCount
        FROM approvalDoc d
        LEFT JOIN approvalDocType t ON d.docTypeId = t.docTypeId
        LEFT JOIN approvalLine l     ON d.docId = l.docId
        LEFT JOIN approvalReference r ON d.docId = r.docId
        WHERE d.docStatus != 'DRAFT'
          AND (
              d.writerEmpId = #{empId}
           OR (l.apprEmpId = #{empId} AND d.docStatus != 'CANCELED')
           OR (r.refEmpId  = #{empId} AND d.docStatus != 'CANCELED')
          )
        <if test="keyword != null and keyword != ''">
            AND d.title LIKE '%' || #{keyword} || '%'
        </if>
        <if test="startDate != null and startDate != ''">
            AND d.regDate &gt;= TO_DATE(#{startDate}, 'YYYY-MM-DD')
        </if>
        <if test="endDate != null and endDate != ''">
            AND d.regDate &lt; TO_DATE(#{endDate}, 'YYYY-MM-DD') + 1
        </if>
        ORDER BY d.docId DESC
        OFFSET #{offset} ROWS FETCH NEXT #{pageSize} ROWS ONLY
    </select>

    <select id="countAll" parameterType="map" resultType="int">
        SELECT COUNT(DISTINCT d.docId)
        FROM approvalDoc d
        LEFT JOIN approvalLine l      ON d.docId = l.docId
        LEFT JOIN approvalReference r ON d.docId = r.docId
        WHERE d.docStatus != 'DRAFT'
          AND (
              d.writerEmpId = #{empId}
           OR (l.apprEmpId = #{empId} AND d.docStatus != 'CANCELED')
           OR (r.refEmpId  = #{empId} AND d.docStatus != 'CANCELED')
          )
        <if test="keyword != null and keyword != ''">
            AND d.title LIKE '%' || #{keyword} || '%'
        </if>
        <if test="startDate != null and startDate != ''">
            AND d.regDate &gt;= TO_DATE(#{startDate}, 'YYYY-MM-DD')
        </if>
        <if test="endDate != null and endDate != ''">
            AND d.regDate &lt; TO_DATE(#{endDate}, 'YYYY-MM-DD') + 1
        </if>
    </select>

    <!-- 문서 상세 조회 -->
    <select id="getDoc" parameterType="long" resultType="com.mvc.app.domain.dto.ApprovalDocDto">
        SELECT d.docId, d.docTypeId, d.title, d.content, d.detailData,
               d.docStatus, d.writerEmpId, d.writerEmpName,
               d.writerDeptCode, d.writerDeptName,
               d.writerGradeCode, d.writerGradeName,
               TO_CHAR(d.regDate, 'YYYY-MM-DD') AS regDate,
               TO_CHAR(d.submitDate, 'YYYY-MM-DD') AS submitDate,
               t.typeName,
               t.formCode
        FROM approvalDoc d
        LEFT JOIN approvalDocType t ON d.docTypeId = t.docTypeId
        WHERE d.docId = #{docId}
    </select>

    <!-- 결재선 조회 -->
    <select id="getLines" parameterType="long" resultType="com.mvc.app.domain.dto.ApprovalLineDto">
        SELECT lineId, docId, stepOrder,
               apprEmpId, apprEmpName,
               apprDeptCode, apprDeptName,
               apprGradeCode, apprGradeName,
               apprTypeCode, lineStatusCode AS apprStatus,
        	   apprComment,
               TO_CHAR(apprDate, 'YYYY-MM-DD HH24:MI') AS apprDate,
               isDeputy, deputyEmpId, deputyName,
               (SELECT dr.deputyName FROM approvalDeputyReg dr
                WHERE dr.delegatorEmpId = approvalLine.apprEmpId
                  AND dr.isActive = 'Y'
                  AND TRUNC(SYSDATE) BETWEEN dr.startDate AND dr.endDate
                  AND ROWNUM = 1) AS activeDeputyName
        FROM approvalLine
        WHERE docId = #{docId}
        ORDER BY stepOrder ASC
    </select>

    <!-- 첨부파일 조회 -->
    <select id="getFiles" parameterType="long" resultType="com.mvc.app.domain.dto.ApprovalFileDto">
        SELECT fileId, docId, oriFilename, saveFilename, fileSize,
               TO_CHAR(regDate, 'YYYY-MM-DD') AS regDate
        FROM approvalFile
        WHERE docId = #{docId}
        ORDER BY fileId ASC
    </select> 

    <!-- 참조자 조회 -->
    <select id="getRefs" parameterType="long"
            resultType="com.mvc.app.domain.dto.ApprovalRefDto">
        SELECT refId, docId, refEmpId, refEmpName,
               refDeptCode, refDeptName, refGradeCode, refGradeName,
               readYn, TO_CHAR(readDate, 'YYYY-MM-DD HH24:MI') AS readDate,
               refComment, TO_CHAR(refCommentDate, 'YYYY-MM-DD HH24:MI') AS refCommentDate
        FROM approvalReference
        WHERE docId = #{docId}
        ORDER BY refId ASC
    </select>

    <!-- 임시저장 삭제 (하위 테이블 먼저 삭제) -->
    <delete id="deleteFiles" parameterType="long">
        DELETE FROM approvalFile WHERE docId = #{docId}
    </delete>
    <delete id="deleteRefs" parameterType="long">
        DELETE FROM approvalReference WHERE docId = #{docId}
    </delete>
    <delete id="deleteLines" parameterType="long">
        DELETE FROM approvalLine WHERE docId = #{docId}
    </delete>
    <delete id="deleteDoc" parameterType="long">
        DELETE FROM approvalDoc WHERE docId = #{docId} AND docStatus IN ('DRAFT', 'REJECTED')
    </delete>

    <!-- 임시저장/재상신 수정 (문서 UPDATE) -->
    <update id="updateDoc" parameterType="com.mvc.app.domain.dto.ApprovalDocDto">
        UPDATE approvalDoc
        SET title = #{title},
            docTypeId = #{docTypeId},
            detailData = #{detailData, jdbcType=VARCHAR},
            content = #{content, jdbcType=VARCHAR},
            docStatus = #{docStatus},
            docVersion = docVersion + #{versionIncrement},
            updateDate = SYSDATE,
            submitDate = CASE WHEN #{docStatus} = 'PENDING' THEN SYSDATE ELSE submitDate END
        WHERE docId = #{docId}
    </update>

    <!-- 결재취소 (기안자 본인, PENDING 상태에서 전원 WAIT일 때만) -->
    <update id="cancelDoc" parameterType="map">
        UPDATE approvalDoc
        SET docStatus = 'CANCELED'
        WHERE docId = #{docId}
          AND writerEmpId = #{empId}
          AND docStatus = 'PENDING'
          AND NOT EXISTS (
              SELECT 1 FROM approvalLine
              WHERE docId = #{docId}
                AND lineStatusCode != 'WAIT'
          )
    </update>

    <!-- 승인 처리 (현재 순위 결재자만) -->
    <update id="approveDoc" parameterType="map">
        UPDATE approvalLine
        SET lineStatusCode = 'APPROVED',
            apprDate = SYSDATE,
            apprComment = #{comment, jdbcType=VARCHAR}
        WHERE docId = #{docId}
          AND apprEmpId = #{empId}
          AND lineStatusCode = 'WAIT'
          AND stepOrder = (
              SELECT MIN(stepOrder) FROM approvalLine
              WHERE docId = #{docId} AND lineStatusCode = 'WAIT'
          )
    </update>

    <!-- 대결 승인 처리 -->
    <update id="approveDocAsDeputy" parameterType="map">
        UPDATE approvalLine
        SET lineStatusCode = 'APPROVED',
            apprDate = SYSDATE,
            apprComment = #{comment, jdbcType=VARCHAR},
            isDeputy = 'Y',
            deputyEmpId = #{empId},
            deputyName = #{empName}
        WHERE docId = #{docId}
          AND lineStatusCode = 'WAIT'
          AND stepOrder = (
              SELECT MIN(stepOrder) FROM approvalLine
              WHERE docId = #{docId} AND lineStatusCode = 'WAIT'
          )
          AND apprEmpId IN (
              SELECT delegatorEmpId FROM approvalDeputyReg
              WHERE deputyEmpId = #{empId}
                AND isActive = 'Y'
                AND TRUNC(SYSDATE) BETWEEN startDate AND endDate
          )
    </update>

    <!-- 대결 반려 처리 -->
    <update id="rejectDocAsDeputy" parameterType="map">
        UPDATE approvalLine
        SET lineStatusCode = 'REJECTED',
            apprDate = SYSDATE,
            apprComment = #{comment},
            isDeputy = 'Y',
            deputyEmpId = #{empId},
            deputyName = #{empName}
        WHERE docId = #{docId}
          AND lineStatusCode = 'WAIT'
          AND stepOrder = (
              SELECT MIN(stepOrder) FROM approvalLine
              WHERE docId = #{docId} AND lineStatusCode = 'WAIT'
          )
          AND apprEmpId IN (
              SELECT delegatorEmpId FROM approvalDeputyReg
              WHERE deputyEmpId = #{empId}
                AND isActive = 'Y'
                AND TRUNC(SYSDATE) BETWEEN startDate AND endDate
          )
    </update>

    <!-- 대결 보류 처리 -->
    <update id="holdDocAsDeputy" parameterType="map">
        UPDATE approvalLine
        SET lineStatusCode = 'HOLD',
            apprDate = SYSDATE,
            apprComment = #{comment, jdbcType=VARCHAR},
            isDeputy = 'Y',
            deputyEmpId = #{empId},
            deputyName = #{empName}
        WHERE docId = #{docId}
          AND lineStatusCode = 'WAIT'
          AND stepOrder = (
              SELECT MIN(stepOrder) FROM approvalLine
              WHERE docId = #{docId} AND lineStatusCode = 'WAIT'
          )
          AND apprEmpId IN (
              SELECT delegatorEmpId FROM approvalDeputyReg
              WHERE deputyEmpId = #{empId}
                AND isActive = 'Y'
                AND TRUNC(SYSDATE) BETWEEN startDate AND endDate
          )
    </update>

    <!-- 현재 WAIT 순서의 원결재자 empId 조회 -->
    <select id="getCurrentWaitApprover" parameterType="long" resultType="string">
        SELECT apprEmpId FROM approvalLine
        WHERE docId = #{docId}
          AND lineStatusCode = 'WAIT'
          AND stepOrder = (
              SELECT MIN(stepOrder) FROM approvalLine
              WHERE docId = #{docId} AND lineStatusCode = 'WAIT'
          )
    </select>

    <!-- 남은 WAIT 수 확인 -->
    <select id="countRemainingWait" parameterType="long" resultType="int">
        SELECT COUNT(*) FROM approvalLine
        WHERE docId = #{docId} AND lineStatusCode = 'WAIT'
    </select>

    <!-- 문서 최종 승인 -->
    <update id="completeDoc" parameterType="long">
        UPDATE approvalDoc
        SET docStatus = 'APPROVED', completeDate = SYSDATE
        WHERE docId = #{docId}
    </update>

    <!-- 반려 처리 -->
    <update id="rejectDoc" parameterType="map">
        UPDATE approvalLine
        SET lineStatusCode = 'REJECTED',
            apprDate = SYSDATE,
            apprComment = #{comment}
        WHERE docId = #{docId}
          AND apprEmpId = #{empId}
          AND lineStatusCode = 'WAIT'
          AND stepOrder = (
              SELECT MIN(stepOrder) FROM approvalLine
              WHERE docId = #{docId} AND lineStatusCode = 'WAIT'
          )
    </update>

    <!-- 문서 반려 상태 변경 -->
    <update id="rejectDocStatus" parameterType="long">
        UPDATE approvalDoc
        SET docStatus = 'REJECTED'
        WHERE docId = #{docId}
    </update>

    <!-- 보류 처리 -->
    <update id="holdDoc" parameterType="map">
        UPDATE approvalLine
        SET lineStatusCode = 'HOLD',
            apprDate = SYSDATE,
            apprComment = #{comment, jdbcType=VARCHAR}
        WHERE docId = #{docId}
          AND apprEmpId = #{empId}
          AND lineStatusCode = 'WAIT'
          AND stepOrder = (
              SELECT MIN(stepOrder) FROM approvalLine
              WHERE docId = #{docId} AND lineStatusCode = 'WAIT'
          )
    </update>

    <!-- 문서 보류 상태 변경 -->
    <update id="holdDocStatus" parameterType="long">
        UPDATE approvalDoc
        SET docStatus = 'ON_HOLD'
        WHERE docId = #{docId}
    </update>

    <!-- 참조자 코멘트 저장 -->
    <update id="updateRefComment" parameterType="map">
        UPDATE approvalReference
        SET refComment = #{comment},
            refCommentDate = SYSDATE
        WHERE docId = #{docId}
          AND refEmpId = #{empId}
    </update>

</mapper>