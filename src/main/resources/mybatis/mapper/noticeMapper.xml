<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mvc.app.mapper.NoticeMapper">

	<insert id="insertNotice"
		parameterType="com.mvc.app.domain.dto.NoticeDto">
		<selectKey keyProperty="noticenum" resultType="long"
			order="BEFORE">
			SELECT NOTICE_SEQ.NEXTVAL FROM dual
		</selectKey>
		INSERT INTO notice (
		noticenum, subject, content,
		hitcount, regdate, isnotice, state, authorEmpId
		) VALUES (
		#{noticenum},
		#{subject, jdbcType=VARCHAR},
		#{content, jdbcType=VARCHAR},
		0,
		SYSDATE,
		#{isnotice},
		1,
		#{authorEmpId, jdbcType=VARCHAR}
		)
	</insert>


	<update id="updateNotice"
		parameterType="com.mvc.app.domain.dto.NoticeDto">
		UPDATE notice
		SET subject = #{subject, jdbcType=VARCHAR},
		content = #{content, jdbcType=VARCHAR},
		isnotice = #{isnotice},
		updatedate = SYSDATE
		WHERE noticenum = #{noticenum}
	</update>


	<update id="deleteNotice" parameterType="long">
		UPDATE notice
		SET state = 0
		WHERE noticenum = #{noticenum}
	</update>


	<update id="increaseHit" parameterType="long">
		UPDATE notice
		SET hitcount = hitcount + 1
		WHERE noticenum = #{noticenum}
	</update>


	<select id="listNotice" parameterType="map"
		resultType="com.mvc.app.domain.dto.NoticeDto">
		SELECT n.noticenum,
		n.subject,
		n.hitcount,
		n.isnotice,
		n.state,
		TO_CHAR(n.regdate, 'YYYY-MM-DD') AS regdate,
		TO_CHAR(n.updatedate, 'YYYY-MM-DD') AS updateDate,
		n.authorEmpId,
		e2.name AS authorName,
		(SELECT COUNT(*) FROM noticefile f WHERE f.noticenum = n.noticenum) AS
		fileCount,
		(SELECT MIN(f2.filenum) FROM noticefile f2 WHERE f2.noticenum = n.noticenum)
		AS firstFilenum
		FROM notice n
		LEFT JOIN employee2 e2 ON n.authorEmpId = e2.empId
		WHERE n.state = 1
		<if test="keyword != null and keyword != ''">
			AND (n.subject LIKE '%' || #{keyword} || '%'
			OR n.content LIKE '%' || #{keyword} || '%')
		</if>
		ORDER BY n.isnotice DESC, n.noticenum DESC
		OFFSET #{offset} ROWS FETCH NEXT #{pageSize} ROWS ONLY
	</select>


	<select id="countNotice" parameterType="map" resultType="int">
		SELECT COUNT(*)
		FROM notice
		WHERE state = 1
		<if test="keyword != null and keyword != ''">
			AND (subject LIKE '%' || #{keyword} || '%'
			OR content LIKE '%' || #{keyword} || '%')
		</if>
	</select>


	<select id="getNotice" parameterType="long"
		resultType="com.mvc.app.domain.dto.NoticeDto">
		SELECT n.noticenum,
		n.subject,
		n.content,
		n.hitcount,
		n.isnotice,
		n.state,
		TO_CHAR(n.regdate, 'YYYY-MM-DD') AS regdate,
		TO_CHAR(n.updatedate, 'YYYY-MM-DD') AS updateDate,
		n.authorEmpId,
		e2.name AS authorName
		FROM notice n
		LEFT JOIN employee2 e2 ON n.authorEmpId = e2.empId
		WHERE n.noticenum = #{noticenum}
	</select>


	<insert id="insertFile"
		parameterType="com.mvc.app.domain.dto.NoticeFileDto">
		<selectKey keyProperty="filenum" resultType="long"
			order="BEFORE">
			SELECT NOTICE_FILE_SEQ.NEXTVAL FROM dual
		</selectKey>
		INSERT INTO noticefile (
		filenum, savefilename, originalfilename, filesize, noticenum
		) VALUES (
		#{filenum},
		#{savefilename},
		#{originalfilename, jdbcType=VARCHAR},
		#{filesize},
		#{noticenum}
		)
	</insert>


	<delete id="deleteFile" parameterType="long">
		DELETE FROM noticefile WHERE filenum = #{filenum}
	</delete>


	<select id="getFiles" parameterType="long"
		resultType="com.mvc.app.domain.dto.NoticeFileDto">
		SELECT filenum, savefilename, originalfilename, filesize, noticenum
		FROM noticefile
		WHERE noticenum = #{noticenum}
		ORDER BY filenum ASC
	</select>


	<select id="getFile" parameterType="long"
		resultType="com.mvc.app.domain.dto.NoticeFileDto">
		SELECT filenum, savefilename, originalfilename, filesize, noticenum
		FROM noticefile
		WHERE filenum = #{filenum}
	</select>

</mapper>